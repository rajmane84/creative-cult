import { Button } from '@/components/ui/button';

const Page = () => {
  return (
    <div className="font-medium bg-red-300 flex items-center min-h-screen">
      <Button>Click Me</Button>
    </div>
  );
};

export default Page;
